#!/bin/sh

set -e
../xeert show < $srcdir/cert.der | grep "signatureAlgorithm: sha256WithRSAEncryption"
../xeert show < $srcdir/cert.der | grep "tbsCertificate.issuer: C=XX, ST=StateName, L=CityName, O=CompanyName, OU=CompanySectionName, CN=CommonNameOrHostname"
